version: '2'

services:

  app:
    build: .
    container_name: ArepTaller5CRUDSystem
    restart: always
    depends_on:
      - mysql
    environment:
      # la IP de la bd de AWS
      SPRING_DATASOURCE_URL: jdbc:mysql://13.218.247.45:3306/properties?createDatabaseIfNotExist=true
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: secret
    ports:
      - "8080:8080"
    networks:
      - app_network

networks:
  app_network: